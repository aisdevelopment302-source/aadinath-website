/** @type {import('next').NextConfig} */
const nextConfig = {
  // All pages are static — no API routes needed
  // Images in public/ are served as-is
  reactStrictMode: true,
}

module.exports = nextConfig
